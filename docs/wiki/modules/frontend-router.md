# Frontend: router 模块

> 来源目录: `frontend/src/router/`
> 范围: frontend

## 职责
前端路由配置，定义 URL 路径到视图组件的映射（`/` → HomeView，`/about` → AboutView），是页面导航的单一来源。

## 关键文件

| 文件 | 主要符号 |
|------|----------|
| `frontend/src/router/index.ts` | path: '/'、path: '/about' |

## 依赖

- HomeView
