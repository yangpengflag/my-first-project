# Frontend: views 模块

> 来源目录: `frontend/src/views/`
> 范围: frontend

## 职责
页面级视图，对应路由的渲染目标。`HomeView` 为首页（组合 TheWelcome 等组件），`AboutView` 为关于页，是用户可见页面的容器。

## 关键文件

| 文件 | 主要符号 |
|------|----------|
| `frontend/src/views/AboutView.vue` | — |
| `frontend/src/views/HomeView.vue` | Vue SFC |

## 依赖

- TheWelcome
