---
name: dispatching-parallel-agents
description: 当面对 2+ 个独立任务，可以在不共享状态或顺序依赖的情况下并行工作时使用
---

# 并行代理调度

## 概述

将任务委托给具有隔离上下文的专用代理。通过精确构建他们的指令和上下文，确保他们专注于自己的任务。他们不应继承你会话的上下文或历史 — 你精确构建他们所需的内容。这也保留了你自己用于协调工作的上下文。

**核心原则：** 每个独立问题领域调度一个代理。让他们并发工作。

## 何时使用

**适用场景：**
- 3+ 个测试文件因不同根因失败
- 多个子系统独立损坏
- 每个问题可以在不了解其他问题的情况下理解
- 调查之间无共享状态

**不适用场景：**
- 失败是相关的（修一个可能修好其他的）
- 需要理解完整系统状态
- 代理会互相干扰

## 模式

### 1. 识别独立领域

按损坏内容分组失败：
- 文件 A 测试：工具审批流程
- 文件 B 测试：批处理完成行为
- 文件 C 测试：中止功能

每个领域是独立的 — 修工具审批不影响中止测试。

### 2. 创建聚焦的代理任务

每个代理获得：
- **特定范围：** 一个测试文件或子系统
- **清晰目标：** 让这些测试通过
- **约束：** 不要修改其他代码
- **期望输出：** 发现和修复的摘要

### 3. 并行调度

在同一响应中发出所有子代理调度 — 他们并行运行：

```
子代理： "修复 agent-tool-abort.test.ts 失败"
子代理： "修复 batch-completion-behavior.test.ts 失败"
子代理： "修复 tool-approval-race-conditions.test.ts 失败"
# 三个并发运行
```

一次响应中多个调度调用 = 并行执行。一次一个 = 顺序执行。

### 4. 审查与整合

代理返回后：
- 阅读每个摘要
- 验证修复不冲突
- 运行完整测试套件
- 整合所有变更

## 常见错误

| 错误 | 正确做法 |
|------|----------|
| 太宽泛："修复所有测试" | 聚焦："修复 agent-tool-abort.test.ts" |
| 无上下文："修复竞态条件" | 粘贴错误消息和测试名称 |
| 无约束 | "不要修改生产代码" |
| 模糊输出："修好它" | "返回根因和变更摘要" |

## 验证

代理返回后：
1. **审查每个摘要** — 理解变更内容
2. **检查冲突** — 代理是否编辑了相同代码？
3. **运行完整套件** — 验证所有修复协同工作
4. **抽查** — 代理可能犯系统性错误
