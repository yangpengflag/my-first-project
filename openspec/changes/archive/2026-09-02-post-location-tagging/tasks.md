# Tasks — post-location-tagging (P6)

- [x] 6.1 posts 实体增量 `city_id` / `spot_ids`（DB 迁移）
- [x] 6.2 `POST /api/posts` 接受可选地点字段
- [x] 6.3 `GET /api/posts` 增量 `cityId` / `spotId` 过滤 + 索引
- [x] 6.4 详情页相关攻略区接真实聚合（替换 `spots-frontend-mock` mock 占位）
- [x] 6.5 增量更新 `posts` spec（本 change 拥有的 Post 地点关联 Requirement）
- [x] 6.6 `openapi:drift` 校验；`type-check` / `tests` / `build` 全绿
